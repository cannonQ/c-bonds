# Secrets — never committed. .env holds node URL and DUST-ONLY test mnemonics.
.env

# Build artifacts
target/
project/target/
project/project/
.bsp/

# Editor / IDE / Metals
.vscode/
.idea/
.metals/
.mcp.json
.bloop/
project/.bloop/
project/metals.sbt

# Harness/session artifacts
.claude/

# ALL internal working material lives here — kickoffs, evidence packs,
# decisions, layouts, phase/rev transaction ledgers, run logs, build plans,
# strategy drafts. Never pushed. Anything internal goes in working/, and
# then it needs no rule of its own.
working/
*/working/

# Run logs anywhere (safety net — these belong in working/)
*.log
